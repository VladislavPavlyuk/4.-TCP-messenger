<Window x:Class="Client.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reconnect to Server" Height="180" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Enter password to reconnect:" Margin="0,0,0,10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,0,0,10" KeyDown="PasswordBox_KeyDown"/>
        <TextBlock x:Name="ErrorTextBlock" Grid.Row="2" Foreground="Red" Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" Width="75" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="75" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

